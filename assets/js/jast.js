(function($) {

	// Put functions/variables here that you want to keep out of window
	// namespace, but don't need to wait for document.ready

	$(function() {

		// document.ready stuff

	});

})(window.jQuery);
